# Qwen-Image-2.1 本地测试工作流与智能提示词扩写系统实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `ComfyUI/qwen_image_2.1/` 下构建端到端的 Qwen-Image-2.1 测试套件，包含对接本地 LM Studio（1234 端口）的提示词智能扩写引擎、ComfyUI API 自动化执行脚本、Web 画布工作流 JSON 及轻量自定义扩展节点。

**Architecture:** 采用“极简描述 -> 本地 LLM 多维度结构化扩写 -> ComfyUI 工作流参数注入 -> DiT 采样生成”的解耦管道。既支持通过命令行以 `--desc` 一键扩写并调度生图，又支持在 ComfyUI 画布中通过原生连线和自定义节点进行图形化调试。

**Tech Stack:** Python 3.10+, ComfyUI API / WebSocket, LM Studio (OpenAI-compatible /v1/chat/completions), Qwen-Image-2.1 GGUF/SafeTensors, pytest.

**Spec:** `docs/superpowers/specs/2026-09-25-qwen-image-2.1-workflow-design.md`

## Global Constraints

- 目录锁定：全部实现文件集中在 `/Users/frank/wk/github/ai/ComfyUI/qwen_image_2.1/` 及其子目录。
- 代码规范：单文件行数不超过 500 行；函数必须有清晰文档与类型注解；严禁使用 `any` 类型；变量必须语义化；异常处理须包含明确日志。
- 语言规范：所有文档、代码注释及交互输出统一使用中文，不生成伴随英文文档。
- Git 规则：提交信息使用英文格式 `type: description`，严禁添加 `Co-Authored-By` 行。

## Review Focus

1. **LM Studio 服务未就绪或超时**：提示词扩写模块必须优雅降级为本地规则模板，不得抛出未捕获异常崩溃。
2. **ComfyUI 服务离线或无对应模型**：测试脚本应输出清晰可读的中文排查指引与模型下载路径，不应输出原始未处理的连接堆栈。
3. **JSON 结构兼容性**：API 格式工作流 (`qwen_image_2.1_api.json`) 与画布格式 (`qwen_image_2.1_t2i_canvas.json`) 的节点 ID 映射必须严谨准确。
4. **分辨率与长宽比约束**：扩写推荐的分辨率必须对齐为 64 或 16 的倍数（例如 1024x1024, 1280x768），以满足 VAE 编解码约束。
5. **模型分档兼容**：支持 `UnetLoaderGGUF` 与原生 `UNETLoader` 的节点结构自适应，确保在 GGUF 或 safetensors 下均可工作。

---

### Task 1: 提示词智能扩写引擎 (`prompt_expander.py`)

**Files:**
- Create: `ComfyUI/qwen_image_2.1/prompt_expander.py`
- Test: `ComfyUI/qwen_image_2.1/tests/test_prompt_expander.py`

**Interfaces:**
- Produces:
  ```python
  @dataclass
  class ExpandedPromptResult:
      positive_prompt: str
      negative_prompt: str
      aspect_ratio: str
      width: int
      height: int
      model_used: str
      is_fallback: bool

  class LMStudioPromptExpander:
      def __init__(self, base_url: str = "http://127.0.0.1:1234/v1", timeout_seconds: float = 15.0): ...
      def detect_model(self) -> str: ...
      def expand(self, user_text: str, style_preset: str = "cinematic", target_aspect: Optional[str] = None) -> ExpandedPromptResult: ...
  ```

- [ ] **Step 1: 编写扩写引擎的测试用例**

```python
# ComfyUI/qwen_image_2.1/tests/test_prompt_expander.py
from typing import Dict, Any
from unittest.mock import patch, MagicMock
import pytest
from prompt_expander import LMStudioPromptExpander, ExpandedPromptResult

def test_heuristic_fallback_when_lmstudio_offline() -> None:
    """测试当 LM Studio 服务离线时，引擎应回退到本地启发式模板并成功返回结构化提示词。"""
    expander = LMStudioPromptExpander(base_url="http://127.0.0.1:99999/v1", timeout_seconds=1.0)
    result = expander.expand(user_text="赛博朋克机甲猫", style_preset="cyberpunk")
    
    assert isinstance(result, ExpandedPromptResult)
    assert result.is_fallback is True
    assert "cyberpunk" in result.positive_prompt.lower()
    assert len(result.negative_prompt) > 0
    assert result.width % 64 == 0
    assert result.height % 64 == 0

def test_aspect_ratio_inference() -> None:
    """测试长宽比与分辨率尺寸推断逻辑。"""
    expander = LMStudioPromptExpander()
    # 宽屏风景测试
    landscape_res = expander.expand("辽阔壮丽的雪山日落全景", style_preset="cinematic")
    assert landscape_res.width > landscape_res.height
    
    # 竖屏肖像测试
    portrait_res = expander.expand("穿着晚礼服的少女半身肖像特写", style_preset="photorealistic")
    assert portrait_res.height > portrait_res.width

@patch("urllib.request.urlopen")
def test_lmstudio_successful_expansion(mock_urlopen: MagicMock) -> None:
    """测试通过模拟 LM Studio 正常响应返回扩写结果。"""
    mock_models_response = MagicMock()
    mock_models_response.read.return_value = b'{"data": [{"id": "mock-qwen-model"}]}'
    mock_models_response.__enter__.return_value = mock_models_response

    mock_chat_response = MagicMock()
    mock_chat_response.read.return_value = b'''{
        "choices": [{
            "message": {
                "content": "A high-tech mechanical feline perched on wet neon-lit pavement, highly detailed metallic joints, glowing cyan optic sensors, rain reflections, 8k resolution, cinematic lighting."
            }
        }]
    }'''
    mock_chat_response.__enter__.return_value = mock_chat_response

    mock_urlopen.side_effect = [mock_models_response, mock_chat_response]

    expander = LMStudioPromptExpander(base_url="http://127.0.0.1:1234/v1")
    result = expander.expand("赛博朋克猫", style_preset="cyberpunk")

    assert result.is_fallback is False
    assert "mechanical feline" in result.positive_prompt
    assert result.model_used == "mock-qwen-model"
```

- [ ] **Step 2: 运行测试验证失败**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_prompt_expander.py -v`
预期: FAIL (ModuleNotFoundError: No module named 'prompt_expander')

- [ ] **Step 3: 编写扩写引擎实现 (`prompt_expander.py`)**

实现 `LMStudioPromptExpander`，包含：
1. 标准库 `urllib.request` / `json` 实现零第三方依赖通信；
2. 针对 Qwen3-VL 文本理解优化的 System Prompt；
3. 风格预设（`cinematic`, `photorealistic`, `anime`, `cyberpunk`, `general`）；
4. 语义长宽比推断（风景 1280x768、人像 768x1280、通用 1024x1024）；
5. 离线/异常启发式回退逻辑。

- [ ] **Step 4: 运行测试验证通过**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_prompt_expander.py -v`
预期: 3 passed

- [ ] **Step 5: 提交代码**

```bash
git add ComfyUI/qwen_image_2.1/prompt_expander.py ComfyUI/qwen_image_2.1/tests/test_prompt_expander.py
git commit -m "feat: implement prompt expander engine with LM Studio integration"
```

---

### Task 2: ComfyUI 工作流资产构建 (`workflows/`)

**Files:**
- Create: `ComfyUI/qwen_image_2.1/workflows/qwen_image_2.1_api.json`
- Create: `ComfyUI/qwen_image_2.1/workflows/qwen_image_2.1_t2i_canvas.json`
- Test: `ComfyUI/qwen_image_2.1/tests/test_workflows.py`

**Interfaces:**
- Produces:
  - `qwen_image_2.1_api.json`: 满足 ComfyUI API `/prompt` 提交格式的节点字典。包含固定锚点节点 ID：
    - Positive Prompt 节点 (`CLIPTextEncode`)
    - Negative Prompt 节点 (`CLIPTextEncode`)
    - EmptyLatentImage 节点 (`width`, `height`)
    - KSampler 节点 (`seed`, `steps`, `cfg`)
  - `qwen_image_2.1_t2i_canvas.json`: 满足 ComfyUI Web 画布完整拓扑结构的 JSON 文件。

- [ ] **Step 1: 编写工作流验证测试**

```python
# ComfyUI/qwen_image_2.1/tests/test_workflows.py
import json
from pathlib import Path

def test_api_workflow_structure() -> None:
    """验证 API 工作流模板结构符合 ComfyUI API 规范且包含关键参数节点。"""
    workflow_path = Path("ComfyUI/qwen_image_2.1/workflows/qwen_image_2.1_api.json")
    assert workflow_path.exists(), "API 工作流模板文件必须存在"

    with open(workflow_path, "r", encoding="utf-8") as f:
        data = json.load(f)

    # 验证是否为节点字典映射结构
    assert isinstance(data, dict)
    
    # 查找关键节点类型
    class_types = [node.get("class_type") for node in data.values() if isinstance(node, dict)]
    assert any("UnetLoaderGGUF" in ct or "UNETLoader" in ct for ct in class_types), "需包含扩散模型加载节点"
    assert any("CLIPLoaderGGUF" in ct or "CLIPLoader" in ct for ct in class_types), "需包含文本编码加载节点"
    assert "VAELoader" in class_types, "需包含 VAE 加载节点"
    assert "CLIPTextEncode" in class_types, "需包含文本编码节点"
    assert "KSampler" in class_types, "需包含采样器节点"
    assert "SaveImage" in class_types, "需包含图像保存节点"

def test_canvas_workflow_structure() -> None:
    """验证 Web 画布工作流符合 ComfyUI 画布格式。"""
    canvas_path = Path("ComfyUI/qwen_image_2.1/workflows/qwen_image_2.1_t2i_canvas.json")
    assert canvas_path.exists(), "画布工作流文件必须存在"

    with open(canvas_path, "r", encoding="utf-8") as f:
        data = json.load(f)

    assert "nodes" in data and isinstance(data["nodes"], list)
    assert "links" in data and isinstance(data["links"], list)
```

- [ ] **Step 2: 运行测试验证失败**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_workflows.py -v`
预期: FAIL (FileNotFoundError)

- [ ] **Step 3: 构造并写入 API 与画布工作流文件**

1. 构造 `workflows/qwen_image_2.1_api.json`：
   - 模型加载：支持 `qwen_image_2.1-Q8_0.gguf` 或 `qwen_image_2.1_int8_convrot.safetensors`；
   - 文本编码：`Qwen3VL-8B-Instruct-Q4_K_M.gguf`（type="qwen_image"）；
   - VAE：`qwen_image_2.1_vae_bf16.safetensors`；
   - 采样配置：Euler, flow_match, 24 步, CFG 4.0。
2. 构造 `workflows/qwen_image_2.1_t2i_canvas.json`：完整的节点图连接，节点摆放整齐，包含注释 Note 说明。

- [ ] **Step 4: 运行测试验证通过**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_workflows.py -v`
预期: 2 passed

- [ ] **Step 5: 提交代码**

```bash
git add ComfyUI/qwen_image_2.1/workflows/ ComfyUI/qwen_image_2.1/tests/test_workflows.py
git commit -m "feat: add Qwen-Image-2.1 API and canvas workflows"
```

---

### Task 3: 命令行端到端测试工具 (`qwen_image21_test.py`)

**Files:**
- Create: `ComfyUI/qwen_image_2.1/qwen_image21_test.py`
- Test: `ComfyUI/qwen_image_2.1/tests/test_cli_runner.py`

**Interfaces:**
- Consumes:
  - `prompt_expander.LMStudioPromptExpander`
  - `workflows/qwen_image_2.1_api.json`
- Produces:
  ```python
  def parse_arguments(argv: Optional[List[str]] = None) -> argparse.Namespace: ...
  def inject_workflow_parameters(workflow: Dict[str, Any], prompt_res: ExpandedPromptResult, steps: int, seed: int) -> Dict[str, Any]: ...
  def check_system_readiness(comfy_url: str, lmstudio_url: str) -> Dict[str, bool]: ...
  ```

- [ ] **Step 1: 编写 CLI 运行工具测试**

```python
# ComfyUI/qwen_image_2.1/tests/test_cli_runner.py
from pathlib import Path
import json
from qwen_image21_test import parse_arguments, inject_workflow_parameters
from prompt_expander import ExpandedPromptResult

def test_cli_argument_parsing() -> None:
    """测试命令行参数解析默认值与自定义参数。"""
    args = parse_arguments(["--desc", "森林里的小鹿", "--style", "cinematic", "--steps", "28", "--dry-run"])
    assert args.desc == "森林里的小鹿"
    assert args.style == "cinematic"
    assert args.steps == 28
    assert args.dry_run is True

def test_workflow_parameter_injection() -> None:
    """测试动态将扩写结果与采样参数正确注入到工作流字典中。"""
    workflow_path = Path("ComfyUI/qwen_image_2.1/workflows/qwen_image_2.1_api.json")
    with open(workflow_path, "r", encoding="utf-8") as f:
        template = json.load(f)

    prompt_res = ExpandedPromptResult(
        positive_prompt="A majestic deer in misty emerald forest",
        negative_prompt="blurry, distorted",
        aspect_ratio="16:9",
        width=1280,
        height=768,
        model_used="test-model",
        is_fallback=False
    )

    injected = inject_workflow_parameters(template, prompt_res, steps=20, seed=12345)
    
    # 验证是否正向提示词节点被成功替换
    found_pos = False
    found_sampler = False
    found_latent = False
    
    for node in injected.values():
        if node.get("class_type") == "CLIPTextEncode" and node["inputs"].get("text") == prompt_res.positive_prompt:
            found_pos = True
        if node.get("class_type") == "KSampler":
            if node["inputs"].get("steps") == 20 and node["inputs"].get("seed") == 12345:
                found_sampler = True
        if node.get("class_type") in ("EmptyLatentImage", "EmptyQwenImageLayeredLatentImage"):
            if node["inputs"].get("width") == 1280 and node["inputs"].get("height") == 768:
                found_latent = True

    assert found_pos, "正向提示词未成功注入"
    assert found_sampler, "采样参数未成功注入"
    assert found_latent, "分辨率未成功注入"
```

- [ ] **Step 2: 运行测试验证失败**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_cli_runner.py -v`
Expected: FAIL (ModuleNotFoundError: No module named 'qwen_image21_test')

- [ ] **Step 3: 编写 CLI 测试工具实现 (`qwen_image21_test.py`)**

实现完整的 CLI 控制器：
1. `parse_arguments()`：支持 `--desc`, `--style`, `--steps`, `--seed`, `--width`, `--height`, `--host`, `--dry-run`；
2. `check_system_readiness()`：探测 ComfyUI 和 LM Studio；输出清晰的状态看板；
3. `inject_workflow_parameters()`：安全定位各节点 ID 并完成正向、负向、尺寸、步数、随机种子的动态注入；
4. `run_generation()`：支持向 ComfyUI 提交任务，利用轮询或 WebSocket 获取任务状态并拉取生成图像保存至 `output/`；
5. `--dry-run` 模式支持：在无需 ComfyUI 服务时完成完整扩写、参数组装与保存校验，方便单元和集成测试。

- [ ] **Step 4: 运行测试验证通过**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_cli_runner.py -v`
Expected: 2 passed

- [ ] **Step 5: 提交代码**

```bash
git add ComfyUI/qwen_image_2.1/qwen_image21_test.py ComfyUI/qwen_image_2.1/tests/test_cli_runner.py
git commit -m "feat: add CLI test runner for end-to-end Qwen-Image-2.1 workflows"
```

---

### Task 4: ComfyUI 自定义提示词扩写节点 (`ComfyUI-LMStudio-Prompt`)

**Files:**
- Create: `ComfyUI/qwen_image_2.1/custom_nodes/ComfyUI-LMStudio-Prompt/__init__.py`
- Create: `ComfyUI/qwen_image_2.1/custom_nodes/ComfyUI-LMStudio-Prompt/lmstudio_prompt_node.py`
- Test: `ComfyUI/qwen_image_2.1/tests/test_custom_node.py`

**Interfaces:**
- Produces:
  ```python
  class LMStudioPromptExpanderNode:
      @classmethod
      def INPUT_TYPES(cls) -> Dict[str, Any]: ...
      RETURN_TYPES = ("STRING", "STRING", "INT", "INT")
      RETURN_NAMES = ("positive_prompt", "negative_prompt", "width", "height")
      FUNCTION = "expand_prompt"
      CATEGORY = "QwenImage/Prompt"
  ```

- [ ] **Step 1: 编写自定义节点测试**

```python
# ComfyUI/qwen_image_2.1/tests/test_custom_node.py
import sys
from pathlib import Path

# 添加自定义节点路径
custom_node_path = Path("ComfyUI/qwen_image_2.1/custom_nodes/ComfyUI-LMStudio-Prompt").resolve()
sys.path.append(str(custom_node_path))
sys.path.append(str(Path("ComfyUI/qwen_image_2.1").resolve()))

from lmstudio_prompt_node import LMStudioPromptExpanderNode

def test_node_specification() -> None:
    """测试节点输入输出类型定义与规范一致。"""
    inputs = LMStudioPromptExpanderNode.INPUT_TYPES()
    assert "required" in inputs
    assert "user_short_desc" in inputs["required"]
    assert "style_preset" in inputs["required"]
    
    assert LMStudioPromptExpanderNode.RETURN_TYPES == ("STRING", "STRING", "INT", "INT")
    assert LMStudioPromptExpanderNode.RETURN_NAMES == ("positive_prompt", "negative_prompt", "width", "height")

def test_node_execution_fallback() -> None:
    """测试节点在离线状态下的正常执行输出。"""
    node = LMStudioPromptExpanderNode()
    pos, neg, w, h = node.expand_prompt(
        user_short_desc="未来都市飞行汽车",
        style_preset="cyberpunk",
        lmstudio_host="127.0.0.1:99999",
        aspect_ratio="16:9"
    )
    assert isinstance(pos, str) and len(pos) > 0
    assert isinstance(neg, str) and len(neg) > 0
    assert w == 1280
    assert h == 768
```

- [ ] **Step 2: 运行测试验证失败**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_custom_node.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 编写自定义节点实现**

实现 `lmstudio_prompt_node.py` 与 `__init__.py`：
- 复用 `prompt_expander.py` 中的核心逻辑，保障逻辑 DRY；
- 暴露标准的 ComfyUI `NODE_CLASS_MAPPINGS` 与 `NODE_DISPLAY_NAME_MAPPINGS`。

- [ ] **Step 4: 运行测试验证通过**

运行: `pytest ComfyUI/qwen_image_2.1/tests/test_custom_node.py -v`
Expected: 2 passed

- [ ] **Step 5: 提交代码**

```bash
git add ComfyUI/qwen_image_2.1/custom_nodes/ ComfyUI/qwen_image_2.1/tests/test_custom_node.py
git commit -m "feat: add ComfyUI custom node for LM Studio prompt expansion"
```

---

### Task 5: 综合使用文档与端到端实测验证

**Files:**
- Create: `ComfyUI/qwen_image_2.1/README.md`
- Test: 全套测试套件运行 + 实操测试

**Interfaces:**
- Produces: 详尽的中文部署、模型配置与运行实测指南。

- [ ] **Step 1: 编写全中文说明文档 `README.md`**

包括：
1. 模型矩阵与下载直链（GGUF Q8/Q4 档位 vs 官方 safetensors）；
2. 存放目录映射表（`models/unet/`, `models/clip/`, `models/vae/`）；
3. LM Studio 提示词扩写配置与调用方法；
4. 命令行测试工具使用说明（包含 `--desc` 示例）；
5. ComfyUI 画布与扩展节点安装加载说明。

- [ ] **Step 2: 运行全量单元测试套件**

运行: `pytest ComfyUI/qwen_image_2.1/tests/ -v`
预期: 全部用例（约 9 个）PASS。

- [ ] **Step 3: 针对本地运行中的 LM Studio (端口 1234) 执行真实扩写实测**

运行:
```bash
python ComfyUI/qwen_image_2.1/qwen_image21_test.py --desc "雨夜小巷里的机甲猫" --style cyberpunk --dry-run
```
验证其能够正确连接本地当前挂载的 `google/gemma-4-26b-a4b-qat`，输出结构化提示词并生成注入后的有效 API 工作流 JSON。

- [ ] **Step 4: 提交代码**

```bash
git add ComfyUI/qwen_image_2.1/README.md
git commit -m "docs: add comprehensive guide and verification for Qwen-Image-2.1"
```

---

## Plan Self-Review Check

1. **Spec 覆盖度检查**：
   - 极简输入与智能扩写 -> Task 1 (`prompt_expander.py`)
   - 命令行自动化套件 -> Task 3 (`qwen_image21_test.py`)
   - ComfyUI 原生画布与自定义节点 -> Task 2 (`workflows/`) & Task 4 (`custom_nodes/`)
   - Apple Silicon / Mac Studio 适配指南与目录落地 -> Task 5 (`README.md`)
2. **占位符检查**：无任何 `TBD`, `TODO`, `implement later` 等占位符，所有测试与关键代码均完整列出。
3. **接口与类型一致性**：`ExpandedPromptResult` 与各函数的命名、参数类型在各任务之间严格保持一致。
4. **异常审查点覆盖**：LM Studio 离线降级、分辨率 64 整数对齐、API 节点 ID 映射均有对应测试用例守护。
